package com.sirolf2009.necromancy.multipart.part;

/** Whether socket offsets apply on the parent's simulation pose or render pose. */
public enum SocketBindSpace {
    SIMULATION,
    RENDER
}
